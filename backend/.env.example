# Backend Environment Variables
# Copy to backend/.env (local) or into your deployment service config

# Port (local)
PORT=8001

# Admin bootstrap
ADMIN_EMAIL=admin@resumebuilder.com
ADMIN_PASSWORD=admin123
ADMIN_NAME=Admin

# MongoDB Connection (use MongoDB Atlas)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/ats-resume-builder?retryWrites=true&w=majority

# JWT Secret (generate a strong secret)
JWT_SECRET=your-super-secret-jwt-key-here-minimum-32-characters

# Admin Creation Secret (optional for scripted setup)
ADMIN_CREATION_SECRET=create-admin-2024

# Google Gemini AI API Key (Google AI Studio)
GEMINI_API_KEY=your-gemini-api-key

# Firebase Admin SDK (optional)
FIREBASE_PROJECT_ID=your-firebase-project-id
FIREBASE_CLIENT_EMAIL=your-firebase-client-email
# For multiline keys, prefer using scripts/encode-firebase-key.js to produce a single-line value:
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour-Private-Key-Here\n-----END PRIVATE KEY-----"

# Email Configuration (for forgot password)
EMAIL_USER=your-gmail-address@gmail.com
EMAIL_PASS=your-gmail-app-password

# Frontend URL (CORS) — set to your Vercel domain in production
FRONTEND_URL=http://localhost:3001

# Node Environment
NODE_ENV=development